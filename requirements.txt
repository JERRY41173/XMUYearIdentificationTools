# 学年鉴定表自动化处理工具 - 依赖包列表
# 必需依赖
pandas>=1.5.0          # Excel文件读取和数据处理
openpyxl>=3.0.10       # Excel文件操作库
python-docx>=0.8.11    # Word文档操作库
comtypes>=1.1.14       # COM接口库（用于WPS/Office自动化）

# 可选依赖（用于改善用户体验）
colorama>=0.4.6        # 彩色终端输出
