# 学年鉴定表自动化处理工具依赖项 v2.0
# 安装命令: pip install -r requirements.txt

# 必需依赖项
pandas>=1.3.0        # Excel文件读取和数据处理
openpyxl>=3.0.0      # Excel文件操作库
python-docx>=0.8.11  # Word文档操作库
comtypes>=1.1.10     # COM接口库（用于WPS/Office自动化）

# 可选依赖项（用于改善用户体验）
colorama>=0.4.4      # 彩色终端输出
tqdm>=4.62.0         # 进度条显示

# 构建工具（开发时需要）
# pyinstaller>=4.5    # 用于打包成exe文件
